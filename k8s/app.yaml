apiVersion: apps/v1
kind: Deployment
metadata:
  name: vietnest
  namespace: vietnam-rent
  labels:
    app.kubernetes.io/name: vietnest
    app.kubernetes.io/part-of: vietnam-rent-miniapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: vietnest
  template:
    metadata:
      labels:
        app.kubernetes.io/name: vietnest
        app.kubernetes.io/part-of: vietnam-rent-miniapp
      annotations:
        vietnest.teamgenius.ru/rollout-at: "2026-06-29T19:43:09Z"
    spec:
      imagePullSecrets:
        - name: ghcr-pull
      containers:
        - name: vietnest
          image: ghcr.io/vilebody/vietnam-rent-miniapp:main
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: PORT
              value: "8080"
          envFrom:
            - secretRef:
                name: vietnest-app-env
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 300m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: vietnest
  namespace: vietnam-rent
  labels:
    app.kubernetes.io/name: vietnest
    app.kubernetes.io/part-of: vietnam-rent-miniapp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vietnest
  ports:
    - name: http
      port: 80
      targetPort: http
